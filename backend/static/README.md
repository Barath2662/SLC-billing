Place your payment QR image at this exact path:

- qr.png

The invoice template references /static/qr.png by default.
This repository includes a placeholder qr.png so the PDF layout remains stable.
Replace it with your real UPI/payment QR image.
You can override the image source with env var QR_IMAGE_SRC.
