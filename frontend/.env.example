# Backend API URL
# Development: leave empty → Vite proxy handles /api → localhost:5000
# Vercel Production: leave empty → both frontend & backend on same Vercel domain
# Only set if backend is on different domain (e.g., Render, Railway, etc.)
VITE_API_URL=
