# Backend API URL
# Development: leave empty → Vite proxy handles /api → localhost:5000
# Production: Set to your deployed backend URL (e.g., Render, Railway, etc.)
# Example: https://slc-billing-api.onrender.com
VITE_API_URL=
