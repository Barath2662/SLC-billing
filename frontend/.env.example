# Backend API URL
# Development: leave empty → Vite proxy handles /api → localhost:5000
# Vercel Production: /_/backend (backend service is at /_/backend route prefix)
# Leave empty for auto-detection or set explicitly:
VITE_API_URL=/_/backend
